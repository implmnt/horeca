# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
        invisible: true
    name:
        label: macrobit.horeca::lang.field.name
    tags:
        label: macrobit.horeca::lang.field.tags
        relation: tags
        select: name