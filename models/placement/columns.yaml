# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
        invisible: true
    name:
        label: macrobit.horeca::lang.field.name
    tables:
        label: macrobit.horeca::lang.field.tables
        relation: tables
        select: name
    firm:
        label: macrobit.horeca::lang.field.firm
        relation: firm
        select: name