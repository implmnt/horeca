# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
        invisible: true
    tags:
        label: macrobit.horeca::lang.field.tags
        relation: tags
        select: name
    firm:
        label: macrobit.horeca::lang.field.firm
        relation: firm
        select: name