# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    tags:
        label: Tags
        relation: tags
        select: name
    firm:
        label: Firm
        relation: firm
        select: id